# Use exact versions (no caret/tilde ranges)
save-exact=true

# Use package-lock.json
package-lock=true

# Fail on outdated packages
engine-strict=true

# Security
audit=true
audit-level=moderate

# Performance
prefer-offline=false
progress=true